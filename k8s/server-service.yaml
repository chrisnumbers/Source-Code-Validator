apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: server
  name: server
spec:
  type: LoadBalancer
  ports:
    - name: "http"
      port: 80
      targetPort: 8080
  selector:
    io.kompose.service: server
